import 'package:flutter/material.dart';

class AppColors {
  static const appMainColor       = Color.fromARGB(255, 255, 95, 0);
  static const appMainDarkColor   = Color.fromARGB(250, 200, 70, 0);

  static const hintTextColor      = Color.fromARGB(255, 187, 187, 187);
  // static const fadedIconColor     = Color.fromARGB(255, 136, 136, 136);
  static const fadedBlack         = Color.fromARGB(120, 0, 0, 0);

  static const bottomNavbar       = Color.fromARGB(255, 255, 244, 236);

  // ------------------------------------------------------------------------------

  // ── Scaffold & Surfaces ───────────────────────────
  static const scaffoldBackground = Color(0xFFF8F6F3);
  static const cardBackground     = Colors.white;

  // ── Text ──────────────────────────────────────────
  static const textPrimary        = Color.fromARGB(255, 14, 14, 14);
  static const textSecondary      = Color.fromARGB(255, 136, 136, 136);
  static const textHint           = Color(0xFFBBBBBB);
  static const textMuted          = Color(0xFFAAAAAA);

  // ── Borders & Dividers ────────────────────────────
  static const border             = Color.fromARGB(255, 70, 70, 70);
  static const divider            = Color.fromARGB(255, 156, 156, 156);

  // ── Semantic ──────────────────────────────────────
  static const error              = Color(0xFFE53935);
  static const errorLight         = Color(0xFFFFEBEE);
  static const success            = Color.fromARGB(255, 55, 145, 58);
  static const successLight       = Color(0xFFE8F5E9);
  static const warning            = Color(0xFFB45309);
  static const warningLight       = Color(0xFFFFF8E1);

  // ── UI Components ─────────────────────────────────
  static const chipBackground     = Color(0xFFF2F0ED);
  static const inputBackground    = Colors.white;
}